
name: Python Delete Pictures File

on:
  push:
    paths:
    - 'test_delete.py'
    - 'test_delete3.py'
    - 'requirements.txt'
    - '.github/workflows/pythonapp.yml'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      # we should give github deside own speed max-parallel: 1
      matrix:
        python: [3.5, 3.6, 3.7, 3.8, 3.9, 3.10.0, 3.11 ] # currently broken no 64Arch [3.12]
        os: [ubuntu-latest] # we dont need windows currently, but is works currently [ windows-2019]

    steps:
    - uses: actions/checkout@v3
    - name: Setup Python
      uses: actions/setup-python@v4.3.0
      with:
        python-version: ${{ matrix.python }}
    - name: Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip3 install -r requirements.txt
    - name: Test with pytest
      run: |
        pip3 install pytest
        python3 -m pytest test_delete.py
